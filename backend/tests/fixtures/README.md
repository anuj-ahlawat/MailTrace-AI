# Isolated development fixtures

These synthetic RFC messages exercise local forensic rules. They are not production evidence, provider reports, or ML evaluation data. The integration check uses a separate MongoDB database and never inserts these records into `mailtrace_enterprise`.
